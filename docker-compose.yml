services:
  vulcan-notify:
    container_name: vulcan-notify
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/vulcan_notify.db
      - SESSION_FILE=/app/data/session.json
