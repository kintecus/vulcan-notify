services:
  vulcan-notify:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/vulcan_notify.db
      - CREDENTIAL_FILE=/app/data/credential.json
